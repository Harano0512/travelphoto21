<h1> マイページ </h1>
<%= @user.nickname %>
<%= @user.prefecture.name%>

<%= link_to 'アカウント情報を編集する', edit_user_registration_path(@user.id) if user_signed_in? && current_user.id == @user.id %>

<%= "#{@user.nickname}の写真一覧" %>
<% if @posts.present? %>
  <% @posts.each do |post| %>
    <% post.images.each do |image| %>
      <%= image_tag image.variant(resize: '400x400') %>
    <% end %>
  <% end %>

<% else %>
  保存されている写真はありません
<% end %>